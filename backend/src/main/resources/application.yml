spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sensors
    username: postgres
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
